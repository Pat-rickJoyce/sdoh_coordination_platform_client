<%# One way in. What arrived is described in the drawer, in clinical terms;
     the cell does not try to summarise it. %>
<%= link_to "Review referral", "#referral-detail-#{referral.id}",
      class: "small-text fw-semibold",
      data: { bs_toggle: "offcanvas" } %>
